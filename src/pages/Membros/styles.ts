import styled from 'styled-components'

interface I {
  selected: boolean
  inativit: boolean
}

export const Container = styled.div`
  flex: 1;
  display: flex;
  background-color: rgba(28, 28, 197, 1);
  min-height: 100vh;
`
